// This is an example on how to use ACL to make a factorial circuit
// However... ACL is not ready for this yet

fn factorial(num: Int) -> Int {
    if num == 0 {
        return 1;
    }

    return num * factorial(num - 1);
}

fn main() -> Int {
    return factorial(5);
}
